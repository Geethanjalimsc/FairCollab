# API keys and secrets — NEVER upload these
.env

# FAISS index files — too large for GitLab
faircollab_index/

# Python bytecode — not needed
__pycache__/
*.pyc

# Virtual environment — not needed
venv/

# Windows junk files
Thumbs.db

# Google service account credentials — NEVER upload
google_service_account.json

# Live connector config — may contain sensitive URLs
data/live_config.json
